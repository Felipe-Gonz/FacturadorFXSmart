spring.application.name=FacturadorFXSmart

#PostgreSQL
spring.datasource.url = jdbc:postgresql://localhost:5432/fxsmart
spring.datasource.username = postgres
spring.datasource.password = root

#JPA/Hibernate
spring.jpa.hibernate.ddl-auto = validate
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql = true
spring.jpa.open-in-view = false

#Flyway
spring.flyway.enabled = true
spring.flyway.validated-migration-naming = true

#Titulo y servidores
springdoc.swagger-ui.path = /swagger-ui.html
springdoc.api-docs.path = /v3/api-docs
# Filtrar paquetes si quieres
# springdoc.packages-to-scan=com.example.FacturadorFXSmart.controller
# Habilitar YAML en /v3/api-docs.yaml (viene por defecto)


